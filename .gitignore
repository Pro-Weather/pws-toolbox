# Secrets — API keys, webhook URLs and station credentials are never committed
.env
.env.*
*.webhook

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Build / tooling artefacts
node_modules/
lychee-report.md
.lycheecache
